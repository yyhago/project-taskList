<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Task List - Lista de Tarefas</title>
</head>
<body>

    <header>
        <% if (task == null) { %>
            <h1>Task List</h1>
            <span>O melhor lugar para <b>organização de tarefas</b> é aqui!</span>
        <form action="/create" method="post">
            <input type="text" name="task" id="task" placeholder="Adicionar Tarefa...">
            <button id="addButton" type="submit">Adicionar</button>
        </form>
        <% } else { %>
            <h1>Atualizando Tarefa...</h1>
            <span>Atualize sua tarefa e adicione novamente!</span>
        <form action="/updateOne/<%= task._id %>" method="post">
            <input type="text" name="task" id="task" value="<%= task.task %>">
            <button id="updateButton" type="submit">Atualizar</button>
        </form>
        <% } %>
    </header>


    <section id="tasks">
        <ul>
            <% taskslist.forEach((task) =>{  %>
                <li>
                    <%= task.task %> | <%= task.date.toLocaleDateString('pt-br') %>
                    <a href="/getById/<%= task._id %>/update">Editar</a>
                    <a href="/getById/<%= task._id %>/delete">Apagar</a>
                </li>
            <% }) %>
        </ul>
    </section>

    <% if (taskDele) { %>
        <section id="modal">
            <div id="card">
                <a href="/">X</a>
                <h2>ATENÇÃO!</h2>
                <hr>
                <p>Certeza que irá apagar sua tarefa? : <%= taskDele.task %></p>
                <form action="/deleteOne/<%= taskDele._id %>" method="post">
                    <button type="submit">Confirmar ação</button>
                </form>
            </div>
        </section>
        <% } %>
        

    <script src="script.js"></script>
</body>
</html>